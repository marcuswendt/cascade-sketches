{
  "name": "volume-field",
  "private": true,
  "type": "module",
  "devDependencies": {
    "cascade": "npm:@field/cascade@^0.5.1",
    "typescript": "^6.0.0"
  },
  "scripts": {
    "check": "tsc --noEmit",
    "check:graph": "cascade check index.cascade",
    "validate": "cascade validate index.cascade",
    "studio": "cascade .",
    "run": "cascade run index.cascade --frames 1 --out renders/still"
  }
}
